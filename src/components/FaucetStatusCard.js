/* import React from "react";

const FaucetStatusCard = ({ isActive }) => {
  return (
    <div className="flex flex-col items-center bg-warning shadow p-1 mb-2 rounded-3 p-6 w-full max-w-md mx-auto mt-6">
      <div className="relative w-16 h-32 bg-gray-300 rounded-full flex items-center justify-center">
        <div className={`w-10 h-10 rounded-full bg-gray-800 transition-transform duration-300 ${isActive ? "translate-y-[-40px]" : "translate-y-[40px]"}`}></div>
      </div>
      <div className="mt-4 flex items-center gap-2">
        <span className="text-xl font-bold">{isActive ? "ON" : "OFF"}</span>
        <div
          className={`w-4 h-4 rounded-full ${isActive ? "bg-green-500" : "bg-red-500"}`}
        ></div>
      </div>
    </div>
  );
};

export default FaucetStatusCard; */
